#include <iostream>

using namespace std;
int fib(int n);

int main()
{

  int a;
  cout << "a = ";
  cin>>a;

  // calculate the fib(i) from scratch for each i <= a using your recursive function
  cout << endl << "From recursive function" << endl;
    cout << fib(a) << endl;
  cout << endl;
  // int userInput = 0;
  //int R = 0;

  // cout << "Enter a number (*NOTE Enter a negative number to quit): ";
  // cin >> userInput;

  // if(userInput < 0)
  // {
  //  return 0;
 
  // }

  // R = fib(R);
  
  // cout << endl;
  // cout << R;
  // cout << endl;
}


 int fib(int n)
  {
    if(n<=0) return 0;
    else if(n==1) return 1;
    else return fib(n-1)+fib(n-2);  
  }

