#include <iostream>

using namespace std;

struct student
{
  int studentID;
  double gpa;
  string fullName;
  int units;
};

  
int main()
{
  student one;
  student two;
  int studentNum;


  cout << "For what student would you like to enter? (1-2): ";
  cin >> studentNum;

  if(studentNum == 1)
    {
      cout << "Please enter the student ID for student 1: ";
      cin >> one.studentID;
      cout << endl;
      cout << "Please enter the gpa for student 1: ";
      cin >> one.gpa;
      cout << endl;
      cout << "Please enter the full name of the student: ";
      cin >> one.fullName;
      cout << endl;
      cout << "Please enter the amount of units: ";
      cin >> one.units;
      cout << endl;
    
      cout << "Okay so the name for student 1 is " << one.fullName << endl;
      cout << "Their ID is " << one.studentID << endl;
      cout << "The amount of units they are taking are " << one.units << endl;
      cout << "And their gpa is " << one.gpa << endl;
    }
  else
{
  cout << "Please enter the student ID for student 2: ";
  cin >> two.studentID;
  cout << endl;
  cout << "Please enter the gpa for student 2: ";
  cin >> two.gpa;
  cout << endl;
  cout << "Please enter the full name of the student: ";
  cin >> two.fullName;
  cout << endl;
  cout << "Please enter the amount of units: ";
  cin >> two.units;
  cout << endl;

  cout << "Okay so the name for student 1 is " << two.fullName << endl;
  cout << "Their ID is " << two.studentID << endl;
  cout << "The amount of units they are taking are " << two.units << endl;
  cout << "And their gpa is " << two.gpa << endl;

  }
  return 0;
}
